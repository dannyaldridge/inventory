<h1>Login</h1>

<%= form_for(:sessions, :url => sessions_path) do |f| %>
	<div>
		<%= f.label(:name, "Name") %>
		<%= f.text_field :name %>
	</div>
	<div>
		<%= f.label(:password, "Password") %>
		<%= f.password_field :password %>
	</div>
	<div>
		<%= f.submit "Log in" %>
	</div>
<% end %>